{
  "name": "jobpipe-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,ui --prefix-colors cyan,magenta \"cd jobpipe && py -m uvicorn jobpipe.server:app --host 0.0.0.0 --port 8090\" \"cd jobpipe-ui && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
